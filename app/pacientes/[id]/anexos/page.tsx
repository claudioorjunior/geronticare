export default function AnexosPage() {
  return (
    <div className="text-sm">
      <p className="text-slate-600">Anexos do paciente — em construção.</p>
      <p className="mt-2 text-xs text-slate-400">Upload + listagem (router de anexos ainda não implementado).</p>
    </div>
  );
}
